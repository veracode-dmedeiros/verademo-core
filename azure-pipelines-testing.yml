# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- task: veracode-sast-pipeline-analysis-dev@0
  inputs:
    samplestring: 'my pipeline'

- task: veracode-sast-platform-analysis-dev@0
  inputs:
    samplestring: 'platform'

- task: veracode-sast-platform-analysis-sandbox-dev@0
  inputs:
    samplestring: 'asfdasdf'

- task: veracode-sast-platform-analysis-promoting-dev@0
  inputs:
    samplestring: 'promote'
