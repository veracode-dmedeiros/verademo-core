# Starter pipeline

# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger: none

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- task: VeracodeSastPlatformAnalysisPromoting-dev@0
  inputs:
    ConnectionType: 'Service'
    veracodeAPIService: 'asdfsadfasdf'
  
- task: VeracodeSastPipelineAnalysis-dev@0
  inputs:
    ConnectionType: 'Service'
    veracodeAPIService: 'asdfsadfasdf'

- task: VeracodeSastPlatformAnalysis-Dev@0
  inputs:
    PlatformProfileName: "test"
    ConnectionType: 'Credentials'
    apiId: 'asdfasdfasdf'
    apiKey: 'asdfasdfasdf'
  
- task: VeracodeSastPlatformAnalysisSandbox-dev@0
  inputs:
    ConnectionType: 'Credentials'
    apiId: 'asdfasdfasdfasdf'
    apiKey: 'sadfsdfasdfasdf'